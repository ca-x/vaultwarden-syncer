// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/ca-x/vaultwarden-syncer/ent/predicate"
	"github.com/ca-x/vaultwarden-syncer/ent/s3config"
	"github.com/ca-x/vaultwarden-syncer/ent/storage"
)

// S3ConfigQuery is the builder for querying S3Config entities.
type S3ConfigQuery struct {
	config
	ctx         *QueryContext
	order       []s3config.OrderOption
	inters      []Interceptor
	predicates  []predicate.S3Config
	withStorage *StorageQuery
	withFKs     bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the S3ConfigQuery builder.
func (sq *S3ConfigQuery) Where(ps ...predicate.S3Config) *S3ConfigQuery {
	sq.predicates = append(sq.predicates, ps...)
	return sq
}

// Limit the number of records to be returned by this query.
func (sq *S3ConfigQuery) Limit(limit int) *S3ConfigQuery {
	sq.ctx.Limit = &limit
	return sq
}

// Offset to start from.
func (sq *S3ConfigQuery) Offset(offset int) *S3ConfigQuery {
	sq.ctx.Offset = &offset
	return sq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (sq *S3ConfigQuery) Unique(unique bool) *S3ConfigQuery {
	sq.ctx.Unique = &unique
	return sq
}

// Order specifies how the records should be ordered.
func (sq *S3ConfigQuery) Order(o ...s3config.OrderOption) *S3ConfigQuery {
	sq.order = append(sq.order, o...)
	return sq
}

// QueryStorage chains the current query on the "storage" edge.
func (sq *S3ConfigQuery) QueryStorage() *StorageQuery {
	query := (&StorageClient{config: sq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := sq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := sq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(s3config.Table, s3config.FieldID, selector),
			sqlgraph.To(storage.Table, storage.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, s3config.StorageTable, s3config.StorageColumn),
		)
		fromU = sqlgraph.SetNeighbors(sq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first S3Config entity from the query.
// Returns a *NotFoundError when no S3Config was found.
func (sq *S3ConfigQuery) First(ctx context.Context) (*S3Config, error) {
	nodes, err := sq.Limit(1).All(setContextOp(ctx, sq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{s3config.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (sq *S3ConfigQuery) FirstX(ctx context.Context) *S3Config {
	node, err := sq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first S3Config ID from the query.
// Returns a *NotFoundError when no S3Config ID was found.
func (sq *S3ConfigQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = sq.Limit(1).IDs(setContextOp(ctx, sq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{s3config.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (sq *S3ConfigQuery) FirstIDX(ctx context.Context) int {
	id, err := sq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single S3Config entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one S3Config entity is found.
// Returns a *NotFoundError when no S3Config entities are found.
func (sq *S3ConfigQuery) Only(ctx context.Context) (*S3Config, error) {
	nodes, err := sq.Limit(2).All(setContextOp(ctx, sq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{s3config.Label}
	default:
		return nil, &NotSingularError{s3config.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (sq *S3ConfigQuery) OnlyX(ctx context.Context) *S3Config {
	node, err := sq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only S3Config ID in the query.
// Returns a *NotSingularError when more than one S3Config ID is found.
// Returns a *NotFoundError when no entities are found.
func (sq *S3ConfigQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = sq.Limit(2).IDs(setContextOp(ctx, sq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{s3config.Label}
	default:
		err = &NotSingularError{s3config.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (sq *S3ConfigQuery) OnlyIDX(ctx context.Context) int {
	id, err := sq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of S3Configs.
func (sq *S3ConfigQuery) All(ctx context.Context) ([]*S3Config, error) {
	ctx = setContextOp(ctx, sq.ctx, ent.OpQueryAll)
	if err := sq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*S3Config, *S3ConfigQuery]()
	return withInterceptors[[]*S3Config](ctx, sq, qr, sq.inters)
}

// AllX is like All, but panics if an error occurs.
func (sq *S3ConfigQuery) AllX(ctx context.Context) []*S3Config {
	nodes, err := sq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of S3Config IDs.
func (sq *S3ConfigQuery) IDs(ctx context.Context) (ids []int, err error) {
	if sq.ctx.Unique == nil && sq.path != nil {
		sq.Unique(true)
	}
	ctx = setContextOp(ctx, sq.ctx, ent.OpQueryIDs)
	if err = sq.Select(s3config.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (sq *S3ConfigQuery) IDsX(ctx context.Context) []int {
	ids, err := sq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (sq *S3ConfigQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, sq.ctx, ent.OpQueryCount)
	if err := sq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, sq, querierCount[*S3ConfigQuery](), sq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (sq *S3ConfigQuery) CountX(ctx context.Context) int {
	count, err := sq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (sq *S3ConfigQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, sq.ctx, ent.OpQueryExist)
	switch _, err := sq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (sq *S3ConfigQuery) ExistX(ctx context.Context) bool {
	exist, err := sq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the S3ConfigQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (sq *S3ConfigQuery) Clone() *S3ConfigQuery {
	if sq == nil {
		return nil
	}
	return &S3ConfigQuery{
		config:      sq.config,
		ctx:         sq.ctx.Clone(),
		order:       append([]s3config.OrderOption{}, sq.order...),
		inters:      append([]Interceptor{}, sq.inters...),
		predicates:  append([]predicate.S3Config{}, sq.predicates...),
		withStorage: sq.withStorage.Clone(),
		// clone intermediate query.
		sql:  sq.sql.Clone(),
		path: sq.path,
	}
}

// WithStorage tells the query-builder to eager-load the nodes that are connected to
// the "storage" edge. The optional arguments are used to configure the query builder of the edge.
func (sq *S3ConfigQuery) WithStorage(opts ...func(*StorageQuery)) *S3ConfigQuery {
	query := (&StorageClient{config: sq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	sq.withStorage = query
	return sq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Endpoint string `json:"endpoint,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.S3Config.Query().
//		GroupBy(s3config.FieldEndpoint).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (sq *S3ConfigQuery) GroupBy(field string, fields ...string) *S3ConfigGroupBy {
	sq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &S3ConfigGroupBy{build: sq}
	grbuild.flds = &sq.ctx.Fields
	grbuild.label = s3config.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Endpoint string `json:"endpoint,omitempty"`
//	}
//
//	client.S3Config.Query().
//		Select(s3config.FieldEndpoint).
//		Scan(ctx, &v)
func (sq *S3ConfigQuery) Select(fields ...string) *S3ConfigSelect {
	sq.ctx.Fields = append(sq.ctx.Fields, fields...)
	sbuild := &S3ConfigSelect{S3ConfigQuery: sq}
	sbuild.label = s3config.Label
	sbuild.flds, sbuild.scan = &sq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a S3ConfigSelect configured with the given aggregations.
func (sq *S3ConfigQuery) Aggregate(fns ...AggregateFunc) *S3ConfigSelect {
	return sq.Select().Aggregate(fns...)
}

func (sq *S3ConfigQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range sq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, sq); err != nil {
				return err
			}
		}
	}
	for _, f := range sq.ctx.Fields {
		if !s3config.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if sq.path != nil {
		prev, err := sq.path(ctx)
		if err != nil {
			return err
		}
		sq.sql = prev
	}
	return nil
}

func (sq *S3ConfigQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*S3Config, error) {
	var (
		nodes       = []*S3Config{}
		withFKs     = sq.withFKs
		_spec       = sq.querySpec()
		loadedTypes = [1]bool{
			sq.withStorage != nil,
		}
	)
	if sq.withStorage != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, s3config.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*S3Config).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &S3Config{config: sq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, sq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := sq.withStorage; query != nil {
		if err := sq.loadStorage(ctx, query, nodes, nil,
			func(n *S3Config, e *Storage) { n.Edges.Storage = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (sq *S3ConfigQuery) loadStorage(ctx context.Context, query *StorageQuery, nodes []*S3Config, init func(*S3Config), assign func(*S3Config, *Storage)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*S3Config)
	for i := range nodes {
		if nodes[i].storage_s3_config == nil {
			continue
		}
		fk := *nodes[i].storage_s3_config
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(storage.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "storage_s3_config" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (sq *S3ConfigQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := sq.querySpec()
	_spec.Node.Columns = sq.ctx.Fields
	if len(sq.ctx.Fields) > 0 {
		_spec.Unique = sq.ctx.Unique != nil && *sq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, sq.driver, _spec)
}

func (sq *S3ConfigQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(s3config.Table, s3config.Columns, sqlgraph.NewFieldSpec(s3config.FieldID, field.TypeInt))
	_spec.From = sq.sql
	if unique := sq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if sq.path != nil {
		_spec.Unique = true
	}
	if fields := sq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, s3config.FieldID)
		for i := range fields {
			if fields[i] != s3config.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := sq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := sq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := sq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := sq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (sq *S3ConfigQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(sq.driver.Dialect())
	t1 := builder.Table(s3config.Table)
	columns := sq.ctx.Fields
	if len(columns) == 0 {
		columns = s3config.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if sq.sql != nil {
		selector = sq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if sq.ctx.Unique != nil && *sq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range sq.predicates {
		p(selector)
	}
	for _, p := range sq.order {
		p(selector)
	}
	if offset := sq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := sq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// S3ConfigGroupBy is the group-by builder for S3Config entities.
type S3ConfigGroupBy struct {
	selector
	build *S3ConfigQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (sgb *S3ConfigGroupBy) Aggregate(fns ...AggregateFunc) *S3ConfigGroupBy {
	sgb.fns = append(sgb.fns, fns...)
	return sgb
}

// Scan applies the selector query and scans the result into the given value.
func (sgb *S3ConfigGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sgb.build.ctx, ent.OpQueryGroupBy)
	if err := sgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*S3ConfigQuery, *S3ConfigGroupBy](ctx, sgb.build, sgb, sgb.build.inters, v)
}

func (sgb *S3ConfigGroupBy) sqlScan(ctx context.Context, root *S3ConfigQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(sgb.fns))
	for _, fn := range sgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*sgb.flds)+len(sgb.fns))
		for _, f := range *sgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*sgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// S3ConfigSelect is the builder for selecting fields of S3Config entities.
type S3ConfigSelect struct {
	*S3ConfigQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ss *S3ConfigSelect) Aggregate(fns ...AggregateFunc) *S3ConfigSelect {
	ss.fns = append(ss.fns, fns...)
	return ss
}

// Scan applies the selector query and scans the result into the given value.
func (ss *S3ConfigSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ss.ctx, ent.OpQuerySelect)
	if err := ss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*S3ConfigQuery, *S3ConfigSelect](ctx, ss.S3ConfigQuery, ss, ss.inters, v)
}

func (ss *S3ConfigSelect) sqlScan(ctx context.Context, root *S3ConfigQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ss.fns))
	for _, fn := range ss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
